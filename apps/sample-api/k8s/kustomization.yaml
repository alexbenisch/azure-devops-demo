apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: app
# Der app-Namespace wird von gitops/infrastructure/configs verwaltet (läuft davor).
resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - ingress.yaml
